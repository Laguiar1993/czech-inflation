# Approved cleanup — 7 September 2026

Scope approved by Luis: clean and stabilize the existing models while preserving forecasting formulas and historical results. Work against an immutable copy of the uncommitted R4 tree, not just HEAD 1251fd8.

1. Freeze inputs and run the existing tests offline. Record the Python and numerical-library versions. Independently reproduce the food seasonal-adjustment cutoff and scoring counterexamples; keep model repairs in a separate queue.
2. Move the shared enrichment loaders into data/struct_inputs.py without changing their function bodies; retain imports in the research script for compatibility. Remove the unreferenced duplicate root components.py and fetchers.py only after checking imports and entry points.
3. Extract the repeated weighted-wedge and trailing-year calculation without changing operation order. Remove the unused config import and redundant past-error slices. Do not consolidate distinct ridge/forest normalization or change the alcohol seed.
4. Make scoreboards import-safe and location-independent. Preserve historical metric arithmetic and release table values. Correct evidence labels: timing alone cannot certify prospective skill, and final-release calls cannot count toward first-release performance.
5. Replace the obsolete front-page guide, preserve it in docs/history, add a short active-model map and explicit live-readiness blockers. Add the missing DuckDB dependency, a development requirements file and a recorded numerical runtime. Support explicit DB/X13 paths while preserving defaults.
6. Run a full before/after 90-origin replay, all horizons/columns, using the same frozen data and runtime. Verify input/output hashes, numeric identity and offline edge cases. Include the replay harness and evidence so Fable can repeat the check.
7. Apply only reviewed files to the original repository after filesystem permission and a hash check against the initial snapshot. Preserve all R4 WIP. Do not create a commit or change model specification. Deliver a review report and exact changed-file list.

Acceptance: existing tests pass; new import/evidence/path tests pass; the full replay and historical release table are unchanged; no frozen data or historical outputs are overwritten in the original repo. This is a research checkpoint, not live certification while the listed timing defects remain.
