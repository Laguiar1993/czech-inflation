# Independent Czech CPI implementation plan

Approved basis: the 9 September review and Luis's instruction to implement it directly, keep forecast expectations outside the main nowcast, test sentiment separately, and investigate the path models. No additional design approval is required for the bounded work below.

Goal: an independently reproducible nowcast family and a corrected, honestly labelled inflation-path research system, with measured comparisons and a portable run entry point.

Architecture: preserve the existing model outputs as a reference. Separate forecast input policy, sequential error histories, event accounting, and release/vintage eligibility. Keep one active independent baseline and a small labelled challenger roster. Historical latest-vintage experiments never qualify as real-time evidence.

## 1. Numerical correctness

- [x] Add failing deterministic tests for transition lag, target changes, known lag propagation, BVAR horizon and predictor-unit invariance; then correct `models/trend_gap.py`, `models/gap_model.py`, `models/bvar.py`. Use calendar-contiguous indexes and explicit failed-fit status/fallback.
- [x] Add a malformed prospective-announcement test: a row with missing gas and a legacy value must not fire in documented mode; preserve complete existing rows' values. Correct both `_gate_fires` and `_gate_value` in `cz_struct.py`.
- [x] Add a complete-quarter aggregation test: known July/August plus forecast September, and suppression of incomplete final quarters. Use the helper in the live CNB display.

## 2. Independent nowcast experiment and live use

- [x] Add `models/independent_nowcast.py` with explicit input policies: `hard` removes exp12/exp36/exp12_x_state/household_exp/esi; `sentiment` keeps only ESI from those five; `legacy` preserves the reference. Inflation expectations remain excluded from both independent policies. CPI/LFS observed statistics are not banned merely because they originate in a statistical survey.
- [x] Build each independent policy's sequential expanding ridge errors from its own origin inputs (48-observation minimum), enforce outcome publication, and fit the existing warm residual forest to those errors. Never reuse the legacy error file for an independent variant.
- [x] Run BASE/HALF/FULL for hard and sentiment on the same 90 release-eve origins and preserve the legacy trio. Score ordinary MAE/RMSE, large surprises >=0.4 pp, material absolute-error gain >=0.15 pp, alerts >=0.2 pp, and paired block-bootstrap differences.
- [x] Main policy defaults to hard. Retain ESI only if it improves common-window RMSE >=2%, with no >2% MAE deterioration and no >5% 2024+ RMSE deterioration; report this as an exploratory practical selection rule, not a significance test. Keep the sentiment line visible as a research comparison regardless.
- [x] Add a shared pure calculation entry using one frozen `as_of` input object. Log independent main, challengers, and optional survey-conditioned comparison with unrounded points and policy-specific sequential-error provenance. Freeze run inputs and completion time; add offline replay with configurable DB/X13 paths.

## 3. Historical data and path comparisons

- [x] Implement a long-form vintage store/selector keyed by series, reference month and available_from. Try to recover genuine historical CZSO unemployment releases; retain source URLs and file hashes. Never relabel 2026 trend-cycle rows as historically observed. Provide a clear latest-vintage research fallback if archives do not cover the long window.
- [x] Build a fixed-input corrected path experiment: independent no-driver target/trend baseline; one small channel model using unemployment/activity, FX and import prices; correctly aligned BVAR; a restrained nonlinear forest challenger. Reuse full headline history for estimation, compare only common finite target rows, and separate real-time-vintage from latest-vintage samples.
- [x] Preserve F1b and survey trend as references; rebuild the independent h0 and core/food bridge on matching clocks. Include all months needed to compound exact YoY and quarter averages. Future drivers are explicit assumptions, preserving already-known lagged information.
- [x] Evaluate horizon-specific monthly and YoY errors, conditional h0-known diagnostics separately from full ex-ante paths, common CNB report-date comparisons, bias, crisis/recent frames, convergence/fallback rates. Do not select on closeness to CNB.

## 4. Energy/event accounting and portability

- [x] Implement/test an all-month energy bill ledger challenger with monetary credits, VAT, commodity/distribution charges, start/expiry, publication gating and exposure scenarios. Reject incompatible units; keep gross event contributions separate from baseline energy contributions. Do not replace the existing historical calendar with guessed sources.
- [x] Run documented scenario/sensitivity checks on existing 2021–23 policy events and show what exposure data remain unavailable. This layer is a challenger until actual CPI mapping is validated.
- [x] Integrate new code under explicit versioned names; add model registry, data manifest, run instructions, before/after report and portable evidence bundle. Run complete tests and offline replay; review the changes before recording implementation commits. Original checkout and its uncommitted work remain preserved.

Execution is in the isolated `codex/independent-cpi-20260909` worktree. Scoped subagents may implement independent mathematical/data/ledger tasks under the applicable subagent-development workflow; root owns nowcast policy, integration and final validation. Research outcomes that fail their gates remain research results rather than being silently promoted.

## Execution record

Completed 9 September 2026. The channel experiment uses genuine origin-vintage
unemployment and FX. Import-price transmission is retained in the independently
rebuilt component bridge; a separate aggregate import extension was not added
because its historical vintage coverage is not established. This is a deliberate
scope adjustment, not a claim that every potential variable has been tested.

The additional user question on optimization was implemented as two bounded
nested experiments: 12 core ridge/window configurations and four residual
forests with zero/half/full corrections. Neither earned promotion. Empirical
sequential interval diagnostics were also added; calibration is not certified.

All 230 tests passed (three retained X-13 diagnostic warnings). The final saved
example reproduces all nine nowcast points and all 91 monthly path points.
Nowcast parity against the independent 90-origin backtest is within
1.2e-16 pp; main contributions sum exactly. The source/runtime guard is checked
again after calculation. See output/R9_VERIFICATION.json and the review documents.

The 454 original supplied files remain byte-identical. Upstream uncommitted
research was first checkpointed as 8eeda3b on the isolated branch. These changes
remain on that branch; there is no merge or push into the original checkout.
The final portable ZIP preserves exact source bytes and includes the reviewed
archive, dependency versions, source evidence and Git history bundle. The database
and Census X-13 executable remain external runtime/data requirements.
