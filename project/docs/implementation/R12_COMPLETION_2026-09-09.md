# R12 implementation completion record

The immutable declaration is R12_EXPERIMENT_PLAN_2026-09-09.md (b0ac3f9).
Detailed pooling parameters were committed in cccb3ca; energy and four path
specifications in abd1342, before their corresponding empirical runs.

Completed implementation scope:

- Three category estimates: separate common-lag control, moderate pooling,
  stronger pooling; baseline/own-category replay and unchanged non-core legs.
- Two long-food replacements: causal centred seasonality/local trend, and a
  fixed upstream agricultural/PPI correction using independently checked lags.
- Matched direct monthly/cumulative headline targets with exact annual accounting.
- All-month evidence wrapper for existing monetary energy machinery; complete
  origin-by-origin eligibility, no qualifying strict historical additions.
- Full prespecified score frames and control comparisons, error attribution,
  source/output hashes, tests and independent spec/correctness review.
- Research roster and portable entry guide, preserving all old numerical models,
  inputs and outputs. No online retailer collection or operating promotion.

The final relevant R9–R12 regression run passed **339 tests**, with two retained
X13 diagnostic warnings. New tests observed the missing implementations first.
Review/debugging corrected constant-column scaling, leading category-history
alignment, canonical weight ordering, live-log hashing, verifier CSV type handling,
publication-calendar DST arithmetic, and optional Git provenance in extracted
folders. No correction was selected to improve an observed forecast score.

The constant-scaling, DST, verifier and Git fixes preserve the empirical forecast
values. The detailed independent review records final hash/arithmetic closure.
The external delivery receipt additionally records a fresh fit from the extracted
ZIP with network/database access blocked. Packaging verification is performed
after the source commit, so its resulting archive hash is intentionally external.

Results: moderate pooling all-headline RMSE 0.406533 versus original categories
0.408947; its core error is slightly worse and some headline gain is error offset.
Large-surprise MAE 0.497043 still trails FULL 0.476061. No path variant beats the
component bridge robustly; full h12 RMSE 5.394839 for the bridge versus 5.691348
food trend, 5.714753 food cost, 6.588405 direct monthly and 6.563285 cumulative.
All 90 strict energy forecasts retain BASE because required mapping is unavailable.

This closes the bounded research round. Further cumulative-core, richer macro
blockwise models, live category integration and populated energy exposures remain
separate work; none is represented as already tested or operational here.
