# R17 energy policy and annual-index conditioning — prefit specification

This research branch preserves all old numerical paths and source files. Main h0 remains HARD_BASE. No national administered-price candidate is certified by the source audit: historical contract shares, fixed expenditure denominators and an identified baseline energy subpath remain absent. The working implementation returns explicit ineligibility, dated policy factors and assumption-labelled bills; it never derives exposures from realised CPI or adds a gross policy effect on top of an unidentified baseline.

## Bill and policy contracts

Reuse immutable R9 Bill/PolicyEvent monetary arithmetic. Add sourced 2021 VAT start/expiry, with CPI treatment available only December10EODUTC. New tariff versions replace earlier versions in the same field after both publication and treatment gates; they do not multiply together. A known expiry removes only that policy. Unknown expiry does not imply a January reversal. POZE charges use min(capacity rate*ceil(breakerA)*phase count*billingmonths,495*fixed MWh over the same billing interval), then allocation to monthly bills. Reject separate POZE adjustment when a total-regulated multiplier includes it. National saving compensation subtracts from a comparable aggregate expenditure denominator once; unavailable denominators fail closed. Caps apply to commodity only, VAT applies once, unmatched populations are never renormalized to complete coverage.

Export all34 source facts and per-origin knowledge/eligibility decisions; preserve original source URLs, date evidence and mappings. Illustrative bills use fixed scenarios (1/3.5/10 MWh annually, supplier fixed fees0/100/200 CZK monthly, reset exposure0/.5/1). These are assumption sensitivities, not fitted/exact CPI outcomes. Keep legal facts separate from these numbers. The old2022 caps' fixed-fee provision remains unimplemented for a national point forecast unless exact contract applicability is sourced.

## Fuel experiment

Bill illustration clarification before full run: the84.70capacity rate is sourced for2024. Its use in illustrative December2025 bills is a carry-forward assumption, not a verified2025tariff. All three predeclared quantities bind the495volume ceiling, so the illustration's result is the declared cap-binding scenario. This must remain in each exported bill row.

Reuse each original R14 origin's saved separately estimated petrol/diesel ECM parameters and same dated petrol share, tax reconciliation and observable pump/oil/FX states. Verify source manifests, forecast clocks and training-availability endpoints. A frozen-spot ECM replay must match R14 before adding any new scenario. FAST's original future fuel control is the operational comparator.

New conditioning input is the latest positive finite FSBTY1 Index observation in the frozen20260911 Bloomberg daily snapshot, assuming usable next calendar day Prague; require age<=31days. It is a user-specified annual index whose constant12month maturity and exact roll mapping remain unverified. Do not call it a delivery curve. No current BDP values enter history. Origin-known TTF spot and TTFGCY1 get separate commodity scenarios, with eligible quote dates and same-date FX; no automatic gas-to-electricity or gas-to-household-bill link.

Fuel scenarios: constant-origin spot; log-linear Brent spot to the annual-index price; annual endpoint*.8 and*1.2. Low/high are sensitivities, never probability bands or selection candidates. Oil follows the original R14 known/frozen series through the decision date; interpolate only later daily dates from the current eligible EIA spot to the endpoint at the final day of t+12. FX remains the same R14 origin-observed level. Seven-day lagged average CZK crude costs feed the existing ECM recurrence. Quote age, source date, missing FX, negative price and any original-fit fallback are explicit. Missing annual quote returns constant-spot ECM with a flag; no row deletion. Monthly fuel is the dated-share weighted average of product relatives after averaging weekly gross price levels, exactly R14's existing convention.

This is a test of annual-price conditioning with a declared interpolation, not a replication of a monthly futures curve. It is not a calibrated war scenario. Same90 origins, h1..12, independent h0 unchanged. Save monthly/weekly prices, parameters reused, input clocks, fallback reasons, own fuel error and recomposed headline paths. No parameters are tuned on these results.
