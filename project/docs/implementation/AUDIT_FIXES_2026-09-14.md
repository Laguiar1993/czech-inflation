**Audit corrections — implementation plan, 14 September 2026**

The design is the correction list in `REVIEW_TO_CLAUDE_2026-09-14.md`, with the user's explicit exclusion of the basket-hour issue. This batch implements those corrections and measures their consequences. New core forecasting experiments and changes to the model roster follow this batch; they are not silently mixed into the correction results.

Work in the existing isolated `codex/independent-cpi-20260909` checkout. Preserve unrelated uncommitted work and existing raw data, forecasts, historical manifests and audit evidence. Save corrected results under `output/audit_fixes_20260914/`; save pre-edit copies and execution logs under `work/audit_fixes_20260914/`. Do not commit someone else's unfinished files as part of this batch.

Implementation uses test-first regression checks and independent review for bounded changes. The audit already supplies reproducible failing examples; convert them into automated tests before applying each fix.

- [x] Fixed transformations: `tools/paper_replication/build_paper_panel.py` and a dedicated test file. Assign T3 explicitly to signed rows 22/25/64; other T2 rows retain log differences with invalid adjacent levels producing missing values, never a global history-dependent switch. The paper and realtime builders use the same declared policy. Test future-observation prefix invariance, exception labels and positive-series equivalence. Rebuild the realtime panel from frozen inputs and compare all cells with the old panel.
- [x] Causal TVW calibration: `models/paper_tvwqrf.py` (shared calibration helper) and `paper_tvwqrf_experiment.py`. Fit the final forest once per origin; generate its mean/quantiles without an internal look-ahead validation block. Estimate combination weights from the latest 12 previously generated quantile forecasts with target month no later than the current edge. Fit each forecast's preprocessing only with information available at its own origin. Generate the required warmup origins for new runs; use fixed feasible weights and an explicit diagnostic when history is short. Test future-target invariance, maturity boundaries, complete chronological validation windows, warmup coverage and unchanged QRF means/quantiles.
- [x] Corrected result comparison: add an offline command that recalibrates saved quantiles using the same production calibration helper, preserving old forecasts and metadata. Report paired before/after monthly and path scores for FULL with and without calendar month. Verify unchanged means and no source-file changes. This is sufficient for the weight-only correction; run a small actual forest refit to exercise the integrated new runner.
- [x] Nowcast scoring: add directional and materially useful alert statistics without changing the established magnitude counts. Test the October-2022 wrong-way example and floating-point threshold boundaries. Write corrected scoreboards separately; preserve all point forecasts.
- [x] Import replay: regenerate the five import-extension CSVs into an audit directory and require byte equality with the original outputs. Final implementation is simpler than the initial plan: preserve the old manifest as a dated file, refresh the active manifest from the verified exact replay and write a receipt binding both hashes. The loader remains unchanged and strict; no compatibility bypass was added. The NFC fix is retained.
- [x] Artifact: label historical replay clocks clearly, retain object (a) only for comparable forecasts, assert unique clocks and forest reconciliation, score unrounded values, and save the corrected page separately. Test missing object-(a) coverage and malformed/duplicate clock inputs. Do not treat the old remote page as updated.
- [x] Documentation and verification: qualify the paper approximation and mixed-origin YoY diagnostic; correct the 83% claim in the handoff with an explicit review addendum. Run affected suites, rerun the two previously blocked checks in a working local dependency environment, review the combined diff and publish exact results and any remaining limitations.

Test command pattern in this workspace: bundled Python3.12 with the existing `work/pythonlibs` and audit-local missing dependencies on PYTHONPATH, `python -m pytest -q -p no:cacheprovider --basetemp <new audit-owned directory> <affected tests>`. Never reuse a pytest base directory containing user data. Runtime versions are recorded in each new result manifest; this environment is not claimed to be the original pinned Python3.14 environment.

Acceptance: demonstrated timing/transform counterexamples stop reproducing; unchanged forecast families remain unchanged; reweighting uses only matured past forecasts; score semantics distinguish magnitude, direction and material gain; all original files outside the scoped code/doc changes remain intact; numerical gains or losses are reported without changing promotion rules.

