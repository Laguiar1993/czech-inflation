# Handoff — CZ CPI nowcast, session 2026-09-05 (Claude)

Briefing for an external reviewer (Codex). Everything below happened after your
`czech_cpi_experiment` bundle was delivered. Repo state: commits `160af6e` and
`3a6259f` on top of `7840f70`. Your bundle's conventions (evaluate.score(),
regular survey ≤2024-12 + flash 2025-01+, first-release actuals, big surprise
≥0.4pp, alert ≥0.2pp) were adopted verbatim for every comparison here.
**Your opinion is wanted on the numbered questions at the end.**

## 1. Your audit finding was verified and fixed (commit 160af6e)

Your README's target-construction claim was correct. `data/local_adapter.py`
built `core_admin = (headline − food_w·food)/(1−food_w)` — fuel never
subtracted, so the "ex-fuel" QRF target contained fuel at measured slope
0.0350 (algebra predicts 0.0347) while the h0 recombination re-added measured
fuel at `fuel_w=0.035`. Fixed using the official CZSO 0722 fuel index
(coicop '07'/subgroup '0722', full 2015-01+ depth — no history lost). The
weekly 60/40 petrol/diesel pump blend validates against the official 0722
index at corr 0.998 (RMSE 0.28 on std 3.91) — first direct check of that
mechanism.

Effect (n=102 own-window origins 2018-02..2026-07, PCA-select primary; a
same-day control rerun of the OLD construction reproduced 0.760/0.819 exactly,
so data drift is nil): **RMSE 0.760 → 0.738**. Under your survey-window
scoring: all60 0.929→0.908, common48 0.890→0.849, 2024+ 0.287→0.271;
big-surprise direction hits unchanged 11/19. New champion file:
`output/backtest_h0_hybrid.csv` (fixed target, mf=1.0).

## 2. Evaluation rulebook agreed with the user (applies to everything below)

- **Drop-frame Januaries**: any month where a model's output is materially
  identical to its benchmark is excluded from comparisons (no credit for
  copying). A January "gate" (output=consensus in Jan) was tested and
  REJECTED — it flatters RMSE via inherited consensus errors and destroys
  Jan-2024, the model's best big-surprise call.
- **Two leagues**: independents scored on RMSE/MAE + directional; survey-
  anchored models scored ONLY where they genuinely disagree (your
  Signed_ElasticMacro 9/13 was re-read this way: median deviation 0.023pp,
  0 alerts ≥0.2pp in 48 months, the −2.3pp Oct-2022 "hit" carried a −0.01pp
  tilt — sign-of-noise; its independent base got 4/13).
- Under this rulebook, ex-January (55m), the fixed hybrid is the best
  independent model in the combined table (0.508 vs your best ElasticMacro
  0.581 — its all60 lead was entirely Jan-2022+Jan-2023); consensus (0.443)
  is unbeaten by any of ~40 variants from both stacks.

Leaderboard page (shareable): https://claude.ai/code/artifact/4c8ca9dd-d81f-46f1-af52-e3e93dc73aec

## 3. Encompassing analysis (surprise ~ β·(model − median), no intercept)

- Fixed hybrid: all60 β=−0.055 (t=−0.7); 2024+ β=+0.28 (t=1.2); ex-ALL-Jan
  β=+0.086 (t=0.4); 2024+ ex-Jan β=−0.09. Every estimate is hostage to 1-2
  months (Jan-2023 destroys, Jan-2024 rescues) → 60 obs cannot resolve β.
- Two-channel split (fuel-measured deviation vs QRF deviation): ex-Jan
  β_fuel=+0.17 (t=0.5), β_qrf=+0.09 (t=0.4) — right signs, n-starved.
- Your Signed recipe applied to OUR hybrid (the one Signed_* you didn't run):
  common48 MAE 0.265 vs consensus 0.260 — no edge either.

## 4. New builds (commit 3a6259f) — all scored under the rulebook

### 4a. Admin shock-gate value bound — the decisive result
Expanding 2σ gate on CNB's January regulated-price MoM series
(`fetch_cnb_regulated_prices_mm_live`; Jan values 2018-26:
1.3, 2.3, 1.5, 0.5, **17.1**, **30.9**, 5.8, 0.8, −0.9). Gate: fires when
|reg_Jan − mean(prior Jans)| > 2σ(prior Jans); adjustment =
0.202 × (reg_Jan − prior mean). Fires on exactly Jan-2022 (+2.99pp) and
Jan-2023 (+5.59pp), nothing else (2024's 5.8 correctly doesn't fire — the
expanding σ includes the shock years by then).

| | all60 RMSE | big hits | Jan-22 err | Jan-23 err |
|---|---|---|---|---|
| Hybrid (fixed) | 0.908 | 11/19 | −3.36 | −4.88 |
| + shock gate | **0.499** | 12/19 | −0.37 | +0.71 |
| Consensus | 0.437 | — | −0.60 | −0.20 |

Realized January regulated MoM is used as an *announcement proxy* (ERÚ
decisions were public by December each year). This is presented as an
information-value BOUND justifying the real `admin_announcements.csv`
calendar build (ERÚ + legislated tobacco excise + municipal water + fares),
not as a production model.

### 4b. LateInfo tilt (`late_info_tilt.py`) — first consensus-beating number
Monteforte–Moretti encompassing template, twice redesigned on user review:
(i) pre-release-window commodity *moves* price month M+1, not the target
month M — Brent/TTF window features removed; (ii) the DE HICP flash for M
arrives ~8-10 days before the CZ release, so analysts see it before
submitting — it cannot be post-close information and was removed too (its
β was +0.06, t=0.76 — empirically dead as well, plus a final-for-flash
vintage caveat). Final spec, 100% domestic + survey-internal:
1. CZSO pump-price tail (last two weekly prints inside M, 60/40 blend, CZK —
   excise/subsidy-inclusive, which Brent structurally is not),
2. survey mean−median skew.
Expanding shrunk ridge (prior strength 4, ≥16 past releases, tilt clip ±0.3).

Results (n=44 evaluable, = 2023-01+): RMSE 0.286 vs consensus 0.256 over the
full evaluable window, **but 2024+ (n=31): 0.231 vs consensus 0.241, big hits
4/5**, mean |tilt| 0.069pp (~3× the Signed models' deviations), 8 alerts
≥0.2 of which 4 correct; full-window big hits 5/9. **Feature betas:
fuel_tail ≈ 0** (the street has pump prices absorbed by release — consistent
with your FastPriceRidge mid-pack result), **skew β=−2.40 (t=−2.19) — the
only statistically significant surprise predictor found in either stack.**
(The de_hicp_mm column stays in the desktop `late_info_features.csv`
snapshot for audit; it is not in the spec.) Skew is
nonzero in only 14/60 months (9/14 sign-correct, 5/7 on bigs) — a ~3×/year
signal. A SurpriseRF variant (same features + hybrid deviation + Jan dummy)
overfits: 13 deviations ≥0.1, 2 correct, 0/8 bigs — documented negative.
STATUS: exploratory (spec postdates the sample); goes to live shadow.

### 4c. Four paper-derived ex-fuel component models (`backtest_h0_variants.py`)
Same origins/recombination as the champion; own-window h0 RMSE (n=102):

| Model | h0 RMSE | MAE | Source idea |
|---|---|---|---|
| X13_QRF | **0.732** | **0.364** | own 4-way food result (X-13 on target −26%) |
| champion TVWQRF | 0.738 | 0.387 | — |
| WIDERAW | 0.803 | 0.437 | Medeiros et al raw-panel RF (h1 tie did NOT port) |
| CSR | 0.860 | 0.453 | Elliott/Gargano/Timmermann 2013 |
| BBIM-lite | 0.880 | 0.491 | BoE SWP 1143 blockwise monotone boosting |

X13_QRF: per-origin one-sided X-13 (fit only through the origin; target-month
factor = mean of that calendar month's fitted factor, last 3 years; scored vs
NSA). Survey-window: worse in the surge (ex-Jan55 0.585 vs 0.508) but wins
2024+ (0.258 vs 0.271, 4/5 bigs) and **alert precision 62-64% vs the
champion's 44%** → adopted as ensemble member, not replacement.
**½TVWQRF+½X13 + admin gate: all60 0.513 / MAE 0.324; 2024+ 0.246 vs
consensus 0.241 — an entirely consensus-free stack at statistical parity.**
CSR/BBIM modules kept (`models/csr.py`, `models/bbim_lite.py`); BBIM's use
case is the demand/supply/trend decomposition, not accuracy.

### 4d. Enriched-panel A/B (commit 9c6754e) — slow block rejected for the nowcast
Six mechanism-picked additions (services CPI mm, 7-staple national farmgate
basket from CZSO CEN0203B, heating-oil weekly CEN0201F; plus M3, HPI,
construction works PPI), tested SHORT (fast-transmission trio) vs FULL
(+ slow trio) on identical origins after the user challenged the
money/housing rationale. Champion PCA-QRF: FULL hurts (own-window 0.749 vs
0.738; survey-window exJan 0.567 vs 0.508 — disqualifying), confirming the
transmission-lag argument. X13+FULL posts a best-ever own-window 0.692, but
the gain concentrates in pre-survey years and dies in the honest frames
(2024+ 0.259 vs base 0.258) — NOT adopted; its 2024+ alert precision (6/8)
is the best recorded. Champion/ensemble unchanged; M3, housing and mortgage
rates reassigned to the h6/h12 roster. Data snapshots with per-row
`available_from` stamps + `AVAILABILITY_README.md` (rules, not vintages —
explicitly labeled) are in the desktop Czech folder. Two pre-result data
bugs caught: label mismatch (basket was silently wheat-only) and mixed
national/regional rows in CEN0203B (now `UZ02HU.KRAJ IS NULL`).
Extra question for you (7): **does the X13+slow-block interaction (0.692
own-window; deseasonalized target leaving the persistent component that
demand variables track) merit a designed test, or is it regime artifact?**

### 4e. Surprise-capture scoreboard — the user's primary objective, formalized
User directive (verbatim intent): beating the survey on average RMSE is not
the goal; the goal is that **when the print is far from the survey, we are
meaningfully closer** — "direction right but 0.0001 closer" is not a win.
Metrics adopted: per-big-event **capture** = (model−survey)/(actual−survey);
**conditional MAE on big months** (survey's = mean |surprise| = 0.605);
**materiality W-L** (win = ≥0.15pp closer to the print than the survey; ties
worthless); **wrong-way deviation count** (|dev|≥0.1 with wrong sign);
Σ(dev×surprise) as tiebreak. Overall RMSE demoted to a calm-month guardrail.

All60, 19 big events:

| Model | med capture | ≥25% events | cond MAE | W-L-T@0.15 | wrong-way |
|---|---|---|---|---|---|
| Stack (½champ+½X13+gate) | 0.11 | **9** | **0.532** | **8-5-6** | 5 |
| Champion+gate | 0.12 | 6 | 0.586 | 6-4-9 | 5 |
| Champion ungated | 0.09 | 5 | 0.744 | 5-5-9 | 6 |
| X13_QRF | 0.09 | 7 | 0.735 | 7-4-8 | 5 |
| LateInfo (n=9) | 0.00 | 1 | 0.485 | 1-1-7 | 2 |
| ElasticMacro | −0.20 | 7 | 0.755 | 6-10-3 | **11** |
| Signed_ElasticMacro | 0.05 | **0** | 0.606 | 0-0-13 | 0 |

2024+ (5 events): Stack med capture 0.28, 2-0-3, zero wrong-way;
ElasticMacro 0-4, 4 wrong-way. Findings this frame exposes: the ungated
champion is FURTHER from the print than the survey on big months; the
gate+X13 blend flips that; LateInfo's RMSE edge is a hugger's edge (its
±0.3 clip caps capture by construction — design change queued: scale the
clip, or move the tilt on top of the Stack); your Signed_ElasticMacro shows
0 material events in 13. Showcase captures (Stack): 2022-09 survey +0.2 →
print +0.80, stack +0.83 (105%); 2023-12 0.0→−0.40, stack −0.37 (92%);
2021-09 −0.2→+0.20, stack +0.16; 2022-05 +1.4→+1.80, stack +1.75; 2024-01
+2.0→+1.50, stack +1.36 (fade); 2022-01 +3.8→+4.40, stack +4.09 (gate).
Honest counterpart: 2023-01 gate overshoots (+6.80 vs +6.00 print) —
catastrophe avoided (−4.9pp → +0.8pp), not a capture win.

User refinement (important): **right-direction overshoot is a WIN** —
survey 4.1 / print 5.0 / forecast 5.6 is a good outcome; the sins are
survey-hugging and wrong-way deviation, never boldness. Verified: the hit
count already credits overshoot (Jan-2024's 128% capture counts); the only
zone where right-way overshoot loses to the survey on error is capture >2×,
which NO model has entered in 19 events (max 1.77). Under the pure
conviction lens — big-month Σ(dev×surprise), no overshoot penalty —
**Champion+gate leads (+0.83) over the Stack (+0.15)** because 50/50
blending halves deviation size, i.e. halves conviction. Accuracy lens →
Stack; conviction lens → Champ+gate. Proposed resolution: point forecast
from Stack, position size from Champ+gate's un-shrunk deviation.

**Question 8**: critique this metric set as the primary model-selection
frame. Materiality threshold 0.15pp (≈ calm-regime survey MAE) — principled
or arbitrary? Is per-event capture ratio robust enough with |s|≥0.4
denominators, or would you use a regression/threshold-weighted CRPS
(Gneiting–Ranjan) instead? Should overshoot stay unpenalized up to 2×
capture (the error-parity boundary), or be scored asymmetrically some other
way? And is "point from Stack, size from gated champion" a coherent
two-output design or does it invite inconsistency? Anything here that
invites gaming?

### 4f. CZ-STRUCT v1 (commit e73e692) — mechanized component model
Response to the user's "great inflation model wrapped in data" directive:
four mechanism blocks + exact-weight recombination (`cz_struct.py`).
Fuel measured (champion's leg, verbatim); admin = expanding calendar-month
median of CNB regulated + January 2σ gate riding the full announced value
(same proxy caveat); food = ridge pipeline on farmgate staples + own lags;
core = expanding ridge Phillips curve on CNB's OWN core series (2007+) with
expectations/wages trend, ESI demand, services persistence, and
STATE-DEPENDENT FX/import pass-through (interactions with 1{trailing yoy
> 4%}); plus an expanding by-month wedge for weight drift/tax effects;
STRUCT_QRF adds a TVW-QRF on the core residual. Timing: uses end-of-month-M
information (FX/ESI/expectations for M) — legitimate at the pre-release
decision time our survey scoring assumes; stricter than it looks (CPI-family
lags M−1, wages M−3, imports M−2, mean-imputed on training slice).

Results: core block validated gate-free — ridge 0.306 vs seasonal-naive
0.500 (target std 0.528). STRUCT_QRF in the proxy-immune ex-Jan frame:
**0.479 = best independent ever** (champion 0.508, Stack 0.522),
materiality 8-2, conviction P&L +0.28 (Stack −0.28); all60 13/19 big hits,
P&L +1.69 (3× Stack). Weaknesses (v1.1 queue): 2024+ RMSE 0.293 vs Stack
0.246 (bolder = noisier, 5 wrong-way ≥0.1); admin normal-January base
contaminated by shock years (2024-01 err −1.04 — needs shock-robust
median); food block 1.046 vs the known-better X13 food treatment (0.990);
gate overshoot undamped (2023-01 +1.15). Own-window 0.448 vs champion
0.773 is gate-flattered (no-gate ablation 0.754) — cite the ex-January
numbers, not that one. **Question 9**: audit `cz_struct.py` for timing
leaks with the same rigor as Q6 (especially the end-of-month-M feature
convention and the wedge/admin expanding constructions), and critique the
state-dependence spec (single 4% threshold dummy vs smooth transition).

### 4g. Gate de-proxied (commit b0c5c1a) — historical announcement calendar built
`data/admin_announcements_history.csv`: sourced ERÚ November decisions,
the cap decree 298/2022 (approved 2022-10-05), and the Dec-2021 supplier
repricings (ČEZ +~33% elec announced 2021-11-01 eff Jan-1; Bohemia Energy
DPI), each with `available_from` preceding the January it prices, and a
stated bill→regulated mapping (×0.55 energy share).
`admin_forecast(mode='announced')` is now the default; the realized-proxy
survives only as `mode='proxy'` for comparison. Fully proxy-free results:
Jan-2022 error **−0.05** (consensus −0.64) — supplier announcements alone
sufficed; Jan-2023 −1.77 (mapping underestimates the cap/subsidy-withdrawal
arithmetic vs realized +30.9 — still ~64% of a shock the ungated model
missed by −4.9). STRUCT_QRF proxy-free: own-window 0.466 (champion 0.773),
all60 0.538 w/ 13/19 bigs W-L 9-3, exJan 0.479 unchanged. This closes the
proxy caveat attached to every January claim above (4a's bound remains the
upper limit; the announced version is the deployable floor). **Question 1
is superseded** — critique instead the mapping (×0.55 share; cap-year
bottom-up needed?) and the ≥8% firing threshold.

### 4h. Survey history extended to 200 releases (commit d33f57b) — STRUCT wins the untouched window
The 60-month window was a pull choice, not a data limit (user catch). Deep
CZCPMOM pull 2010-01..2026-08: byte-identical on the 60m overlap; 2025+
contamination re-confirmed (exact-match 0.83-0.88 vs ~0.2 prior years) so
the flash file stays authoritative there; canonical merged table =
`data/czcpmom_survey_history_extended.csv`. Aug-2026: actual +0.3
(released 2026-09-04); its survey column is settled-snapshot-suspect and
excluded from scoring. Big events 19 → 25 (23 inside model coverage).

Extended scoreboard (2018-02..2026-07, 23 events): **STRUCT_QRF leads every
capture column** — condMAE 0.510 (survey 0.578), materiality 11-3,
wrong-way 4 (lowest), PnL +1.26; Stack 0.511/9-6/+0.12; champion 0.715/5-7.
**Pseudo-OOS subsample 2018-02..2021-07** (42 months, 4 big events —
predates every selection decision made this week, all of which used
2021-08+): STRUCT_QRF condMAE 0.387 (W-L 2-0, 1 wrong-way) and X13 0.385
(2-0) BEAT the survey; champion 0-2. Caveats: spec design postdates the
era in calendar time even if selection never touched it; n_big=4.
**Question 10**: does this pseudo-OOS window materially de-risk the
overfitting-by-iteration concern, or is architecture-level hindsight
(state threshold, block choices) still contaminating? How would you weight
it vs a true live shadow?

Full-field extended scoring (YOUR independent forecasts start 2019-01, so
your models are scoreable on the new months too — capture columns,
survey condMAE 0.568 on the 23 events):

| model | condMAE | W-L@0.15 | big hits | PnL |
|---|---|---|---|---|
| STRUCT_QRF | **0.504** | **12-3** | **16/23** | **+1.29** |
| Stack | 0.511 | 9-6 | 12/23 | +0.12 |
| X13 | 0.674 | 9-4 | 13/23 | −1.40 |
| ElasticMacro | 0.692 | 7-10 | 8/23 | −1.45 |
| PhysicalBridge | 0.693 | 7-7 | 11/23 | −2.57 |
| MacroRidge | 0.689 | 7-9 | 8/23 | −1.39 |
| PCA_QRF | 0.871 | 3-12 | 7/23 | −3.50 |
| Champion (ungated) | 0.715 | 5-7 | 12/23 | −1.02 |

Pseudo-OOS 2018-21 (4 scoreable events, survey condMAE 0.45): X13 0.385
(2-0) and STRUCT_QRF 0.388 (2-0) beat the survey; ElasticMacro 0.396
(1-0) creditable; champion 0.579 (0-2), PCA_QRF 0.698 (0-3), AR3 0.949.
Note your PCA_QRF — the best Codex model on 2024+ RMSE — is 3-12 on
extended materiality with PnL −3.50: RMSE respectability concealing
harmful surprise behavior, same pattern as ElasticMacro on the 60m window.

v1.1 also landed in `cz_struct.py` (announcement-shock Januaries excluded
from the admin seasonal base; X13 one-sided deseasonalization in the food
block; `struct_live()` shadow logging with SPEC_TAG) — rerun pending a DB
lock; numbers in the repo may be one run behind this note.

### 4i. CZ-STRUCT v1.2 (commit cd058c9) — h=1 win, additive admin, ST rejected
(A) Announced gate is now ADDITIVE (announced energy shock + normal-year
seasonal base for non-energy January repricing): Jan-2023 −1.9→−1.72, h0
STRUCT_QRF 0.472→0.464. (B) Smooth-transition state (logistic, width 1pp)
tested and REJECTED — loses to the hard 4% dummy 0.470 vs 0.467 (your Q9
design question, answered empirically; ST column kept in the output CSV).
(C) **STRUCT_H1**: mechanized h=1 (direct h-step ridges; `known_through`
plumbing so a December origin legitimately sees the January announcement;
seasonal-median fuel): **RMSE 0.517 vs champion TVW-QRF 0.739 on the same
88 target months** — a 30% win in the champion's other domain. Please
leak-audit the h=1 alignment in `_ridge_predict` (h param) and
`admin_forecast(known_through=...)` with the same rigor as Q9.

### 4j. CZ-STRUCT v1.3 (commit 88e483b) — h=3 + bands. **h3 NUMBER RETRACTED in 4k.**
v1.3 reported STRUCT_H3 0.539 (38% win) and claimed the edge "does not
decay with horizon" — **both retracted**: the January gate read the
announcement calendar without an availability check, so an October origin
forecasting January used November's announcement. Superseded by 4k.

### 4k. CZ-STRUCT v1.4 (commit d909217) — self-caught leak fixed; honest horizon map; continuity rule
`_gate_value()` now requires announcement `available_from` <= the origin's
decision date at EVERY horizon (the exact class of flaw Q9 asks you to
hunt — one instance found by self-audit while building h6/12; please hunt
for more). Honest results: **h3 0.660 vs champion 0.874 (24%)**; corrected
sweep h0 40% / h1 30% / h3 24% — **the mechanized edge decays with
horizon** (announcements, pipelines and measurements are short-lived
information; economically sensible). **h6/h12 honest negatives**: with the
slow block (M3+housing) in its literature home, h6 0.886 vs 0.899 (wash),
h12 0.994 vs 0.986 (slight loss) — mechanism does not crack the long end.
Live-continuity rule (user directive: only variables that continue):
de_food_l1 removed (dead 2025-12, Eurostat re-cut; was silently
mean-imputed through 2026 — food block 1.044→1.068, correctness over
fit); freshness tripwire in `struct_live` (>6m lag ⇒ loud warning).
Regime-conditional bands 82→83% — insufficient, t-scaling next. h0 0.462 /
h1 0.514 new bests. SPEC v1.4.

### 4l. CZ-STRUCT v1.5 (commit 56f85e1) — processor stage completes the food pipeline
CEN0201B carries food-products PPI (CZ-CPA division 10, IZ2015, monthly
2015-01+, continuing) — the missing farmgate→processor→shelf middle stage.
Wired as `food_ppi_l1` only (PPI(M) publishes ~16th M+1, after BOTH the
flash and the full print — user-verified — so PPI(M−1), out mid-M, is the
only usable vintage at h0). Food block 1.068→0.913; **h0 0.447 / h1 0.510
/ h3 0.670**; h6/12 unchanged (short-horizon weapon confirmed). Capture
frames improved across the board: extended condMAE 0.506, **W-L 10-2**,
17/23 bigs, **PnL +2.07**; exJan 9-1/+1.19; 2024+ 5/5 bigs. Bands still
81% — the one open calibration item. Please include `load_food_ppi_mm`
and the food-block timing in the leak audit.

### 4m. CZ-STRUCT v1.6 (commit 9fbaae4) — time-varying weights; first sub-consensus RMSE
User directive ("the basket changes — static makes no sense"): per-regime
component weights recovered from the aggregation identity by constrained
OLS on released months (real-time strict; the weights are published
constants, so this estimates a known number). Findings: CNB regulated
share fell 0.28→0.11 over the decade — the static 0.202 was badly stale;
fuel 0.026-0.040 validates the 0.035 fallback; identity residual
0.10→0.071. **Two errors had been cancelling**: the oversized admin
weight was compensating the conservative ×0.55 announcement mapping in
shock Januaries — with true weights, Jan-22/23 worsen (−0.43/−2.26) and
full-window h0 rises 0.447→0.476, while EVERY forward frame improves:
**2024+ RMSE 0.235 vs consensus 0.241 — the first independent model under
consensus in the current regime**; pseudo-OOS 0.265 (condMAE 0.358);
direction 18/23; bands 84%. The residual January error is now cleanly the
mapping's (Q1-supersession item: a bottom-up announced-quantities mapping
is the v1.7 fix, gated on your critique). Note for Q6/Q9 audits: the
recovered weights are *effective* recombination weights for OUR component
definitions (recovered food 0.17-0.22 vs official div-01 0.169-0.178 —
collinearity between food and core shifts the split while tightening the
identity); that is what recombination needs, but critique welcome.

### 4n. Audit round + v1.6.1 official weight anchors (commit 552a9e3) — FINAL STATE FOR REVIEW
User challenged "too good to be true"; audit produced: **(1) the 2024+
sub-consensus claim is retracted to PARITY** — DM p=0.82, bootstrap CI
[−0.040, +0.027]; anywhere earlier sections say "first under consensus",
read "first statistical parity". (2) Weights re-anchored to OFFICIAL
sources: food = CZSO cpi_weights per regime (in-force mapping verified vs
spot_kos2026.pdf), fuel = official 07.22 = 0.03063 (diesel/petrol split
also validates the 60/40 pump blend), admin = only solved share (single-
unknown; the v1.6 recovered food 0.220 was a collinearity artifact vs
official 0.169). Official cross-checks: CZSO strict-regulated 9.0%, CNB
bucket ~11-12%, solved effective 0.161-0.167. (3) Flash-file consensus
verified ex-ante (21% exact-match = base rate; contamination would flatter
consensus, i.e. bias against us). **Final v1.6.1 numbers**: h0 0.482 / h1
0.535 / h3 0.692 vs champion 0.773/0.739/0.874; frames — extended 18/23
bigs, condMAE 0.509 (survey 0.568), W-L 8-3, PnL +1.35; pseudo-OOS 0.272,
condMAE 0.365, 1-0; 2024+ 0.245 ≈ consensus 0.241, 5/5 bigs. Weight-
variant sensitivity for your review: static 0.447 / pure-recovered 0.476 /
official-anchored 0.482 — official adopted on principle over backtest
vanity. Remaining declared risks: iteration-on-seen-data (cure = live
shadow), announcement-mapping author-bias (cure = Nov ERÚ), latest-vintage
predictors (no archive exists; symmetric across models), champion-h1-file
variant (~3pp of that margin). This section supersedes conflicting numbers
in 4f-4m; the audit request list stands (Q6/Q9/Q10 + solve_weights).

## 5. Config resolution (both sessions converged)
`qrf_max_features = 1.0` for point forecasts (1/3 costs +3-5% RMSE at h1/h3,
confirmed independently by both sessions on clean targets); `1/3` retained
ONLY as a second forest for the quantile bands (`qrf_band_max_features`) —
5-95% coverage h1 79%→93%, PIT/KS calibrated. The mid-day "corrupted h1
actuals" scare was a `--synthetic` smoke test transiently overwriting
output/; the real h1 target was never corrupted.

## 6. Data scouting (Czech-only)
- CZSO's preliminary mid-month food price field survey (the fuel-source
  sibling) was TERMINATED end-2025; scanner replacement publishes with the
  CPI. History 2011-2025 remains trainable; live food channel candidates:
  MZe/SZIF weekly market reports, or the e-shop/scanner lane the parallel
  session scouted.
- OTE daily electricity spot fetcher exists, wired into nothing (energy
  repricing beyond January).
- CZ imputed rents follow the acquisition approach → construction-materials
  PPI leads it, not asking rents.
- CNB's quarterly forecast publishes a monthly CPI path — a free second
  anchor (`cb_projection` type) + consensus−CNB disagreement feature.

## 7. Questions for you

1. **Admin bound**: any leakage objection to realized-January-regulated-MoM as
   an announcement proxy (announcements public by Dec)? Would you construct
   the bound differently (e.g., cap the adjustment at the announced-items
   subset)? Is the expanding-2σ gate acceptable as "no tuned threshold"?
2. **LateInfo**: how would you pre-register the live shadow so its 2024+ edge
   (0.227 vs 0.241, n=31) is testable rather than anecdotal — months needed,
   stopping rule, and which metric is primary (RMSE delta vs big-hit rate)?
3. **Skew signal** (β=−2.40, t=−2.19, 14 firing months): real forecaster-
   disagreement information, or multiple-testing residue from ~3 features
   tested? Any literature you'd check it against (forecaster disagreement →
   surprise direction)?
4. **X13_QRF's alert-precision edge** (62-64% vs 44%): plausible mechanism
   (deseasonalized target → better-signed deviations) or artifact? Would you
   adopt the 50/50 ensemble or weight it?
5. **The drop-frame + disagreement-conditioning rulebook**: anything you'd
   push back on before we treat it as the standing evaluation standard?
6. Anything in `backtest_h0_variants.py` / `late_info_tilt.py` /
   `models/csr.py` / `models/bbim_lite.py` you'd flag on correctness — same
   audit you did for the ex-fuel target.

## File map
- Champion: `output/backtest_h0_hybrid.csv` (fixed target); variants:
  `output/backtest_h0_variants.csv`; tilt: `output/late_info_tilt.csv`.
- Code: `backtest_h0_variants.py`, `late_info_tilt.py`, `models/csr.py`,
  `models/bbim_lite.py`, fix in `data/local_adapter.py` (+config comments).
- Fetched-data snapshots for audit (no refetch needed), in
  `C:\Users\luis_\OneDrive\Ambiente de Trabalho\Czech\`:
  `cnb_regulated_prices_mm.csv` (235m, 2007-01..2026-07 — the admin-gate
  input), `late_info_features.csv` (60 releases: release_dt, fuel_tail,
  de_hicp_mm, skew, surprise — the exact tilt feature matrix),
  `de_hicp_headline_mm_fred.csv` (FRED CP0000DEM086NEST, flash proxy).
- Your bundle (unchanged, reference):
  `C:\Users\luis_\Documents\Codex\2026-09-05\...\czech_cpi_experiment\`.
- Commits: `160af6e` (fix), `3a6259f` (builds); `git log -2 --stat` for the file list.
